# TODO - SERVISTA run steps + Windows venv fix

- [ ] Fix Windows dev script to use the correct venv python path (currently hardcoded to venv\Scripts\python.exe)
- [ ] Add a robust setup script (PowerShell) that creates venv if missing and installs requirements using the same python
- [ ] Document updated run steps for backend (Django) and mobile (Expo)
- [ ] Re-run the script to confirm migrations + server start without "python.exe path" errors

